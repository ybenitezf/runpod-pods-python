## ADDED Requirements

None - this is a cleanup-only change.

## MODIFIED Requirements

None - existing pod-lifecycle and list-pods capabilities remain unchanged.

## REMOVED Requirements

None - while template-related code is being deleted, those files were never part of any documented capability.

## Notes

This change removes unused code (template management files) and fixes a misnamed environment variable. No existing capability requirements are affected.
